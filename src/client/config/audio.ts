import type { AudioConfig } from "../../types/index.js";

// Configuración de audio usada por la UI.
export const AUDIO_CONFIG: AudioConfig = {
  // Volumen inicial de la lluvia (0-100).
  DEFAULT_RAIN_VOL: 60,
  // Volumen inicial de música (0-100).
  DEFAULT_MUSIC_VOL: 100,
  // Volumen del sonido de cambio día/noche (0-100).
  DEFAULT_SWITCH_SFX_VOL: 70,
};
